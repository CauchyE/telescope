version: "3"

services:
  cosmoscan:
    image: lcnem/cosmoscan
    # build:
    #   dockerfile: ./Dockerfile
    volumes:
      - ./config.js:/usr/share/nginx/html/assets/config.js
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - 80:80
    restart: always
